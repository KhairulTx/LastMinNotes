# Copy this file to .env.local and fill in real values. NEVER commit .env.local to Git.

# OpenAI (required for AI flashcard generation)
OPENAI_API_KEY=sk-...

# ToyyibPay (required for RM1 payments)
# Get from toyyibpay.com → Login → User Secret Key
TOYYIBPAY_USER_SECRET_KEY=your_secret_key_here
# Create a Category in ToyyibPay dashboard, copy the Category Code
TOYYIBPAY_CATEGORY_CODE=your_category_code_here

# App URL for payment callback (use your production URL on Vercel, e.g. https://your-app.vercel.app)
APP_URL=http://localhost:3000

# JWT secret for access tokens (production: use a long random string, e.g. openssl rand -base64 32)
TOKEN_SECRET=dev-secret-change-in-production

# Upstash Redis / Vercel KV (required for production so session survives after payment redirect)
# From Vercel → Storage → Upstash Redis, or Upstash dashboard
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here

# Optional: Use dev.toyyibpay.com for sandbox testing
# TOYYIBPAY_SANDBOX=1

# Optional: Skip payment for local testing. Remove or set to 0 for production deploy.
# SKIP_PAYMENT_FOR_TEST=1
